# Python venv (never commit — it's huge and machine-specific)
.venv/
__pycache__/
*.pyc

# Secrets
.env

# Generated pipeline outputs (regenerated by the pipeline / weekly refresh)
outputs/*
!outputs/.gitkeep
